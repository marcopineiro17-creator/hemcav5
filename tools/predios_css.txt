<style>
/* ===================================================================
   CPM · REGULARIZACION DE PREDIOS
   Todo el CSS va acotado a #cpm-predios. El contenedor de codigo de
   Hostinger inserta esto dentro de la pagina del constructor: cualquier
   selector suelto le cambiaria el aspecto al editor y al resto del sitio.

   Marca: fondo blanco, tinta azul marino de CPM y el azul fuerte de la
   division legal (#051958) como secundario. Titulos en serif del sistema
   -- formal, y sin depender de un CDN de fuentes que dentro del iframe no
   cargaria.

   Sobre el movimiento: nada de backdrop-filter (cuesta fotogramas incluso
   cuando el elemento esta fuera de pantalla) y nada de rotaciones
   continuas sobre superficies grandes (obligan a rasterizar cada
   fotograma). Lo que se anima es opacity, translate3d, clip-path y
   stroke-dashoffset, que son baratos.
   =================================================================== */
#cpm-predios {
  /* Paleta. El azul viene del bloque de Divisiones, donde ya identifica
     a la division legal: esta pagina no inventa una marca nueva. */
  --azul:#051958;
  --azul-2:#0b2c7d;
  --azul-3:#1e3a8a;
  --navy:#192c41;
  --oro:#b08a3e;
  --tinta:#14213a;
  --texto:#495468;
  --suave:#6b7688;
  --linea:#dfe3ec;
  --linea-fuerte:#c3cbdc;
  --papel:#ffffff;
  --papel-2:#f5f7fb;
  --papel-3:#eef2f9;

  --serif:Georgia,'Times New Roman','Nimbus Roman',serif;
  --sans:'DM Sans','Montserrat',system-ui,-apple-system,'Segoe UI',Roboto,Helvetica,Arial,sans-serif;

  --ancho:1180px;
  --canal:clamp(18px,5vw,56px);

  position:relative;
  background:var(--papel);
  color:var(--texto);
  font-family:var(--sans);
  font-size:16px;
  line-height:1.65;
  -webkit-font-smoothing:antialiased;
  text-align:left;
  overflow:hidden;
}
#cpm-predios *,#cpm-predios *::before,#cpm-predios *::after { box-sizing:border-box }
#cpm-predios img { max-width:100%;height:auto;display:block }
#cpm-predios svg { display:block;max-width:100% }

/* Reinicios acotados: el tema del sitio pone reglas sobre button y a que
   se cuelan aqui dentro (bordes redondeados, sombras, mayusculas). */
#cpm-predios a { color:inherit;text-decoration:none }
#cpm-predios button {
  font:inherit;color:inherit;background:none;border:0;margin:0;padding:0;
  text-transform:none;letter-spacing:normal;box-shadow:none;border-radius:0;
  text-align:left;cursor:pointer;
}
#cpm-predios p,#cpm-predios h1,#cpm-predios h2,#cpm-predios h3,
#cpm-predios h4,#cpm-predios ul,#cpm-predios ol,#cpm-predios figure { margin:0 }
#cpm-predios ul,#cpm-predios ol { padding:0;list-style:none }

/* ---------- Estructura ---------- */
#cpm-predios .env { width:100%;max-width:var(--ancho);margin:0 auto;padding:0 var(--canal) }
#cpm-predios .banda { padding:clamp(56px,8vw,104px) 0;position:relative }
#cpm-predios .banda--claro { background:var(--papel-2) }
#cpm-predios .banda--linea { border-top:1px solid var(--linea) }

/* ---------- Tipografia ---------- */
#cpm-predios .rotulo {
  font-size:11.5px;letter-spacing:.18em;text-transform:uppercase;font-weight:600;
  color:var(--azul);display:flex;align-items:center;gap:10px;margin-bottom:18px;
}
#cpm-predios .rotulo::before {
  content:"";width:26px;height:1px;background:var(--azul);flex:0 0 26px;
  transform-origin:left;transform:scaleX(0);transition:transform .7s cubic-bezier(.22,.61,.36,1) .1s;
}
#cpm-predios .rv.is-visible .rotulo::before,
#cpm-predios .rotulo.rv.is-visible::before { transform:scaleX(1) }
#cpm-predios h1,#cpm-predios h2,#cpm-predios h3,#cpm-predios .cifra {
  font-family:var(--serif);color:var(--tinta);font-weight:400;line-height:1.16;
  letter-spacing:-.01em;
}
#cpm-predios h1 { font-size:clamp(31px,5.1vw,58px) }
#cpm-predios h2 { font-size:clamp(25px,3.6vw,41px) }
#cpm-predios h3 { font-size:clamp(18px,2vw,22px);line-height:1.3 }
#cpm-predios .entradilla {
  font-size:clamp(16px,1.65vw,19px);line-height:1.7;color:var(--texto);max-width:60ch;
}
#cpm-predios .intro-seccion { max-width:64ch;margin-bottom:clamp(32px,4vw,52px) }
#cpm-predios .intro-seccion p { margin-top:16px;font-size:clamp(15.5px,1.5vw,17.5px) }
#cpm-predios strong { color:var(--tinta);font-weight:600 }

/* ---------- Botones ---------- */
#cpm-predios .acciones { display:flex;flex-wrap:wrap;gap:12px;margin-top:32px }
#cpm-predios .btn {
  display:inline-flex;align-items:center;gap:10px;position:relative;overflow:hidden;
  padding:15px 27px;border-radius:2px;font-size:14.5px;font-weight:600;
  transition:color .2s ease,border-color .2s ease,transform .2s ease;
  transform:translateZ(0);
}
#cpm-predios .btn::before {
  content:"";position:absolute;inset:0;z-index:-1;transform:scaleX(0);transform-origin:right;
  transition:transform .34s cubic-bezier(.22,.61,.36,1);
}
#cpm-predios .btn:hover::before { transform:scaleX(1);transform-origin:left }
#cpm-predios .btn:active { transform:translateY(1px) }
#cpm-predios .btn--solido { background:var(--azul);color:#fff }
#cpm-predios .btn--solido::before { background:var(--azul-3) }
#cpm-predios .btn--linea { border:1px solid var(--linea-fuerte);color:var(--tinta);background:#fff }
#cpm-predios .btn--linea::before { background:var(--papel-3) }
#cpm-predios .btn--linea:hover { border-color:var(--azul);color:var(--azul) }
#cpm-predios .btn .flecha { transition:transform .28s cubic-bezier(.22,.61,.36,1) }
#cpm-predios .btn:hover .flecha { transform:translateX(4px) }

/* =====================================================================
   ESCENAS: foto o ilustracion
   Cada escena lleva una capa de foto y una ilustracion vectorial debajo.
   Si en el guion se declara una URL, se anade .con-foto y la foto tapa la
   ilustracion; si no, la ilustracion es lo que se ve, y esta hecha para
   sostenerse sola -- no es un hueco gris esperando una imagen.
   ===================================================================== */
#cpm-predios .escena {
  position:relative;overflow:hidden;background:var(--papel-3);
  border:1px solid var(--linea);
}
#cpm-predios .escena-foto {
  position:absolute;inset:0;background-size:cover;background-position:center;
  opacity:0;transition:opacity .8s ease,transform 1.2s cubic-bezier(.22,.61,.36,1);
  transform:scale(1.06);
}
#cpm-predios .escena.con-foto .escena-foto { opacity:1 }
#cpm-predios .escena.con-foto.is-visible .escena-foto { transform:scale(1) }
#cpm-predios .escena.con-foto .arte { opacity:0 }
/* Tinte azul muy leve, no un velo: da unidad de marca y hace que el filete
   blanco se lea sobre cualquier fotografia, pero deja ver la imagen. Empezo
   siendo un velo al 42% pensado para sostener texto encima; con fotos de
   verdad tapaba justo lo que hay que ver, y las etiquetas que van sobre la
   imagen llevan su propio fondo solido y no lo necesitan. */
#cpm-predios .escena.con-foto::after {
  content:"";position:absolute;inset:0;pointer-events:none;
  background:linear-gradient(160deg,rgba(5,25,88,.2),rgba(5,25,88,.03) 55%,rgba(5,25,88,.26));
}
#cpm-predios .arte { position:absolute;inset:0;width:100%;height:100%;transition:opacity .5s ease }
#cpm-predios .escena-marco {
  position:absolute;inset:16px;border:1px solid rgba(255,255,255,.5);pointer-events:none;z-index:2;
  clip-path:inset(0 100% 0 0);
}
#cpm-predios .escena.is-visible .escena-marco {
  animation:cpmPrediosMarco 1.05s cubic-bezier(.22,.61,.36,1) .25s both;
}
@keyframes cpmPrediosMarco { from{clip-path:inset(0 100% 0 0)} to{clip-path:inset(0)} }

/* --- Trazos del plano: se dibujan solos al entrar --- */
#cpm-predios .trazo {
  stroke-dasharray:var(--largo,600);stroke-dashoffset:var(--largo,600);
}
#cpm-predios .escena.is-visible .trazo {
  animation:cpmPrediosTrazo 1.5s cubic-bezier(.33,1,.68,1) both;
}
#cpm-predios .escena.is-visible .trazo.t2 { animation-delay:.28s }
#cpm-predios .escena.is-visible .trazo.t3 { animation-delay:.48s }
#cpm-predios .escena.is-visible .trazo.t4 { animation-delay:.66s }
#cpm-predios .escena.is-visible .trazo.t5 { animation-delay:.82s }
@keyframes cpmPrediosTrazo { to{stroke-dashoffset:0} }
#cpm-predios .aparece { opacity:0 }
#cpm-predios .escena.is-visible .aparece { animation:cpmPrediosAparece .8s ease .9s both }
#cpm-predios .escena.is-visible .aparece.a2 { animation-delay:1.05s }
#cpm-predios .escena.is-visible .aparece.a3 { animation-delay:1.2s }
@keyframes cpmPrediosAparece { from{opacity:0;transform:translateY(6px)} to{opacity:1;transform:none} }
/* El sello cae y se asienta: es el gesto que cierra el plano. */
#cpm-predios .sello { opacity:0;transform-origin:center;transform-box:fill-box }
#cpm-predios .escena.is-visible .sello { animation:cpmPrediosSello .75s cubic-bezier(.2,1.5,.4,1) 1.3s both }
@keyframes cpmPrediosSello {
  from{opacity:0;transform:scale(1.7) rotate(-14deg)}
  to{opacity:1;transform:scale(1) rotate(-7deg)}
}

/* ---------- Portada ---------- */
#cpm-predios .portada { padding:clamp(48px,7vw,92px) 0 clamp(40px,5.5vw,68px);position:relative }
#cpm-predios .portada::after {
  content:"";position:absolute;left:0;right:0;bottom:0;height:3px;
  background:linear-gradient(var(--azul) 0 1px,transparent 1px 2px,var(--linea) 2px 3px);
}
#cpm-predios .portada-rejilla {
  display:grid;grid-template-columns:minmax(0,1fr) minmax(0,.86fr);
  gap:clamp(30px,4.5vw,64px);align-items:center;
}
#cpm-predios .portada h1 { margin-bottom:22px }
#cpm-predios .portada h1 em { font-style:italic;color:var(--azul) }
#cpm-predios .portada .entradilla { max-width:52ch }
#cpm-predios .portada .escena { aspect-ratio:16/11;min-height:0 }
#cpm-predios .portada-datos {
  display:flex;flex-wrap:wrap;gap:clamp(20px,4vw,56px);margin-top:clamp(34px,4.6vw,52px);
  padding-top:26px;border-top:1px solid var(--linea);
}
#cpm-predios .dato .cifra { display:block;font-size:clamp(21px,2.6vw,28px);color:var(--azul) }
#cpm-predios .dato span {
  display:block;font-size:12.5px;letter-spacing:.09em;text-transform:uppercase;
  color:var(--suave);margin-top:6px;
}

/* ---------- Los dos casos ---------- */
/* Un caso por fila, con la fotografia vertical a un lado y el texto al
   otro, alternando el lado. Antes eran dos columnas con la foto apaisada
   arriba; las fotos de esta division son verticales -- gente midiendo en
   campo, documentos -- y en un hueco apaisado se recortaban justo por
   donde importan. */
#cpm-predios .casos { display:grid;gap:clamp(20px,2.6vw,30px);grid-template-columns:1fr }
#cpm-predios .caso {
  background:#fff;border:1px solid var(--linea);overflow:hidden;
  display:grid;grid-template-columns:minmax(0,.44fr) minmax(0,1fr);
  transition:box-shadow .3s ease;
}
#cpm-predios .caso:hover { box-shadow:0 18px 46px rgba(5,25,88,.12) }
#cpm-predios .caso--espejo { grid-template-columns:minmax(0,1fr) minmax(0,.44fr) }
#cpm-predios .caso--espejo .escena { order:2;border-left:1px solid var(--linea);border-right:0 }
#cpm-predios .caso .escena {
  border:0;border-right:1px solid var(--linea);aspect-ratio:auto;min-height:400px;height:100%;
}
#cpm-predios .caso-cuerpo {
  padding:clamp(26px,3.4vw,44px);display:flex;flex-direction:column;justify-content:center;
}
#cpm-predios .caso-marca {
  position:absolute;left:18px;top:18px;z-index:3;
  font-size:10.5px;letter-spacing:.15em;text-transform:uppercase;font-weight:700;
  color:#fff;background:var(--azul);padding:6px 12px;
}
#cpm-predios .caso-marca--suave { background:#fff;color:var(--azul) }
#cpm-predios .caso h3 { margin-bottom:14px }
#cpm-predios .caso p { font-size:15.5px;margin-bottom:14px }
#cpm-predios .caso-hacemos {
  margin-top:auto;padding-top:20px;border-top:1px solid var(--linea);
  font-size:14.5px;color:var(--texto);
}
#cpm-predios .caso-hacemos b {
  display:block;font-size:11.5px;letter-spacing:.14em;text-transform:uppercase;
  color:var(--azul);margin-bottom:8px;font-weight:700;
}
#cpm-predios .casos-menores {
  display:grid;gap:clamp(16px,2vw,22px);grid-template-columns:repeat(2,1fr);
  margin-top:clamp(18px,2.4vw,26px);
}
#cpm-predios .caso-menor {
  border:1px solid var(--linea);padding:clamp(20px,2.6vw,28px);background:#fff;
  display:grid;grid-template-columns:auto 1fr;gap:18px;align-items:start;
  transition:border-color .25s ease;
}
#cpm-predios .caso-menor:hover { border-color:var(--azul) }
#cpm-predios .caso-menor h3 { font-size:18px;margin-bottom:9px }
#cpm-predios .caso-menor p { font-size:14.8px }
#cpm-predios .icono-marco {
  width:48px;height:48px;flex:0 0 48px;border:1px solid var(--linea);
  display:grid;place-items:center;color:var(--azul);background:var(--papel-2);
}
#cpm-predios .icono-marco svg { width:24px;height:24px }

/* ---------- Servicios ---------- */
#cpm-predios .servicios {
  display:grid;gap:1px;background:var(--linea);border:1px solid var(--linea);
  grid-template-columns:repeat(4,1fr);
}
#cpm-predios .servicio {
  background:#fff;padding:clamp(24px,3vw,32px);position:relative;
  transition:background .25s ease;
}
#cpm-predios .servicio::after {
  content:"";position:absolute;left:0;right:0;bottom:0;height:2px;background:var(--azul);
  transform:scaleX(0);transform-origin:left;transition:transform .3s cubic-bezier(.22,.61,.36,1);
}
#cpm-predios .servicio:hover { background:var(--papel-2) }
#cpm-predios .servicio:hover::after { transform:scaleX(1) }
#cpm-predios .servicio-alto { display:flex;align-items:center;justify-content:space-between;margin-bottom:16px }
#cpm-predios .servicio-num { font-family:var(--serif);font-size:13px;color:var(--azul);letter-spacing:.06em }
#cpm-predios .servicio .icono { width:26px;height:26px;color:var(--azul) }
#cpm-predios .servicio .icono path,#cpm-predios .servicio .icono line,
#cpm-predios .servicio .icono circle,#cpm-predios .servicio .icono rect {
  stroke-dasharray:120;stroke-dashoffset:120;
}
#cpm-predios .servicio.is-visible .icono path,#cpm-predios .servicio.is-visible .icono line,
#cpm-predios .servicio.is-visible .icono circle,#cpm-predios .servicio.is-visible .icono rect {
  animation:cpmPrediosTrazo 1.1s cubic-bezier(.33,1,.68,1) .15s both;
}
#cpm-predios .servicio h3 { font-size:19px;margin-bottom:10px }
#cpm-predios .servicio p { font-size:14.8px;line-height:1.62 }

/* ---------- Alcance ---------- */
#cpm-predios .alcance { display:grid;gap:clamp(30px,4vw,60px);grid-template-columns:1fr 1fr;align-items:center }
/* Diptico: dos fotos verticales dentro de una sola escena, cada una en su
   mitad. Asi las dos se ven completas en vez de recortar una apaisada. */
#cpm-predios .alcance .escena { aspect-ratio:1/1;min-height:320px }
#cpm-predios .escena.es-diptico .escena-foto { width:50%;right:auto }
#cpm-predios .escena-foto-2 {
  position:absolute;top:0;bottom:0;right:0;width:50%;
  background-size:cover;background-position:center;opacity:0;
  transition:opacity .8s ease .1s,transform 1.2s cubic-bezier(.22,.61,.36,1);
  transform:scale(1.06);
}
#cpm-predios .escena.con-foto .escena-foto-2 { opacity:1 }
#cpm-predios .escena.con-foto.is-visible .escena-foto-2 {
  transform:scale(1.04) translate3d(0,calc(var(--par,0px) * -1),0);
}
/* Junta entre las dos mitades, para que se lean como dos laminas. */
#cpm-predios .escena.es-diptico.con-foto::before {
  content:"";position:absolute;left:50%;top:0;bottom:0;width:2px;
  background:#fff;z-index:2;transform:translateX(-1px);
}
#cpm-predios .fichas { display:flex;flex-wrap:wrap;gap:9px;margin-top:26px }
#cpm-predios .ficha {
  border:1px solid var(--linea-fuerte);padding:9px 16px;font-size:14px;color:var(--tinta);
  background:#fff;opacity:0;transform:translateY(8px);
  transition:opacity .5s ease,transform .5s ease,border-color .25s ease,color .25s ease;
}
#cpm-predios .fichas.is-visible .ficha { opacity:1;transform:none }
#cpm-predios .fichas.is-visible .ficha:nth-child(1) { transition-delay:.04s }
#cpm-predios .fichas.is-visible .ficha:nth-child(2) { transition-delay:.09s }
#cpm-predios .fichas.is-visible .ficha:nth-child(3) { transition-delay:.14s }
#cpm-predios .fichas.is-visible .ficha:nth-child(4) { transition-delay:.19s }
#cpm-predios .fichas.is-visible .ficha:nth-child(5) { transition-delay:.24s }
#cpm-predios .fichas.is-visible .ficha:nth-child(6) { transition-delay:.29s }
#cpm-predios .fichas.is-visible .ficha:nth-child(7) { transition-delay:.34s }
#cpm-predios .fichas.is-visible .ficha:nth-child(8) { transition-delay:.39s }
#cpm-predios .ficha:hover { border-color:var(--azul);color:var(--azul) }
#cpm-predios .nota-alcance {
  margin-top:26px;padding:20px 24px;background:var(--papel-3);border-left:3px solid var(--azul);
  font-size:15px;
}

/* ---------- Proceso ----------
   La linea se llena segun avanza el scroll por la seccion. El guion
   escribe --avance (0 a 1) una vez por fotograma; aqui solo se lee. */
#cpm-predios .pasos { position:relative;padding-left:clamp(0px,4vw,64px) }
#cpm-predios .via {
  position:absolute;left:clamp(14px,2vw,26px);top:8px;bottom:8px;width:2px;background:var(--linea);
}
#cpm-predios .via::after {
  content:"";position:absolute;left:0;top:0;width:100%;
  height:calc(var(--avance,0) * 100%);background:var(--azul);
  transform-origin:top;
}
#cpm-predios .paso {
  display:grid;grid-template-columns:auto 1fr;gap:clamp(18px,2.6vw,30px);
  padding:clamp(20px,2.6vw,28px) 0;position:relative;
}
#cpm-predios .paso + .paso { border-top:1px solid var(--linea) }
#cpm-predios .paso-num {
  font-family:var(--serif);font-size:clamp(22px,2.8vw,31px);color:var(--azul);
  line-height:1;min-width:2.4ch;position:relative;
}
#cpm-predios .paso-num::before {
  content:"";position:absolute;left:calc(clamp(14px,2vw,26px) - clamp(18px,4vw,64px) - 4px);
  top:.35em;width:11px;height:11px;border-radius:50%;background:#fff;
  border:2px solid var(--linea);transition:border-color .3s ease,transform .3s ease;
}
#cpm-predios .paso.is-visible .paso-num::before { border-color:var(--azul);transform:scale(1.15) }
#cpm-predios .paso h3 { margin-bottom:8px }
#cpm-predios .paso p { font-size:15.2px;max-width:62ch }

/* ---------- Bloque institucional ---------- */
#cpm-predios .institucional {
  background:var(--azul);color:#dfe5f5;padding:clamp(36px,5vw,64px);
  position:relative;overflow:hidden;
  display:grid;grid-template-columns:minmax(0,1fr) minmax(0,.9fr);
  gap:clamp(26px,3.4vw,46px);align-items:center;
}
#cpm-predios .institucional::before {
  /* Retícula tenue: textura de plano, sin imagenes ni filtros caros. */
  content:"";position:absolute;inset:0;pointer-events:none;opacity:.5;
  background-image:linear-gradient(rgba(255,255,255,.055) 1px,transparent 1px),
                   linear-gradient(90deg,rgba(255,255,255,.055) 1px,transparent 1px);
  background-size:38px 38px;
  mask-image:radial-gradient(circle at 70% 40%,#000,transparent 72%);
  -webkit-mask-image:radial-gradient(circle at 70% 40%,#000,transparent 72%);
}
#cpm-predios .institucional > * { position:relative;z-index:1 }
#cpm-predios .institucional h2 { color:#fff }
#cpm-predios .institucional p { margin-top:18px;font-size:clamp(15.5px,1.5vw,17.5px);max-width:60ch }
#cpm-predios .institucional .rotulo { color:#9fb0dd }
#cpm-predios .institucional .rotulo::before { background:#9fb0dd }
#cpm-predios .institucional-lista {
  display:grid;grid-template-columns:repeat(3,1fr);gap:clamp(16px,2.2vw,26px);
  margin-top:clamp(26px,3.4vw,38px);padding-top:clamp(22px,2.6vw,30px);
  border-top:1px solid rgba(255,255,255,.2);
}
#cpm-predios .institucional-lista h3 { color:#fff;font-size:16.5px;margin-bottom:7px }
#cpm-predios .institucional-lista p { margin-top:0;font-size:14.2px;color:#c4cfe9 }

/* Sello giratorio. Elemento pequeno y sin filtros: la rotacion es barata.
   Se detiene cuando el bloque no esta a la vista (clase hb-portada-lejos
   no aplica aqui; se usa .is-visible, que el motor ya administra). */
#cpm-predios .sello-caja { display:grid;place-items:center;position:relative;aspect-ratio:1;width:100% }
#cpm-predios .sello-anillo { position:absolute;inset:0;animation:none }
#cpm-predios .institucional.is-visible .sello-anillo {
  animation:cpmPrediosGiro 44s linear infinite;
}
@keyframes cpmPrediosGiro { to{transform:rotate(360deg)} }
#cpm-predios .sello-nucleo { position:relative;width:56%;color:#fff }

/* ---------- Preguntas ---------- */
#cpm-predios .preguntas { border-top:1px solid var(--linea);max-width:900px }
#cpm-predios .pregunta { border-bottom:1px solid var(--linea) }
#cpm-predios .pregunta-btn {
  width:100%;display:flex;align-items:flex-start;justify-content:space-between;gap:20px;
  padding:22px 0;font-family:var(--serif);font-size:clamp(17px,1.9vw,20px);color:var(--tinta);
  line-height:1.35;transition:color .2s ease;
}
#cpm-predios .pregunta-btn:hover { color:var(--azul) }
#cpm-predios .pregunta-mas { flex:0 0 18px;width:18px;height:18px;position:relative;margin-top:6px }
#cpm-predios .pregunta-mas::before,#cpm-predios .pregunta-mas::after {
  content:"";position:absolute;background:var(--azul);left:50%;top:50%;
}
#cpm-predios .pregunta-mas::before { width:14px;height:1.5px;transform:translate(-50%,-50%) }
#cpm-predios .pregunta-mas::after {
  width:1.5px;height:14px;transform:translate(-50%,-50%);
  transition:transform .24s ease,opacity .24s ease;
}
#cpm-predios .pregunta.abierta .pregunta-mas::after { transform:translate(-50%,-50%) scaleY(0);opacity:0 }
/* La altura no se anima: dentro del iframe cada fotograma obligaria a
   remedir y redimensionar el marco, que es el temblor que costo quitar en
   los otros bloques. El contenido si entra con un desvanecido. */
#cpm-predios .pregunta-cuerpo { display:none;padding:0 0 24px;max-width:72ch }
#cpm-predios .pregunta.abierta .pregunta-cuerpo {
  display:block;animation:cpmPrediosAparece .42s ease both;
}
#cpm-predios .pregunta-cuerpo p { font-size:15.4px }
#cpm-predios .pregunta-cuerpo p + p { margin-top:12px }

/* ---------- Cierre ----------
   La imagen va como banda propia, limpia: sin velo azul y sin degradado.
   Antes iba de fondo con un velo encima para que el texto se leyera, pero
   la foto que cierra la pagina es un predio real con una casa vieja y el
   punto es justamente que se vea tal cual. Poniendola como banda, la
   imagen se ve entera y el texto conserva su contraste sobre blanco. */
#cpm-predios .cierre { position:relative;padding:0 0 clamp(56px,7vw,92px);text-align:center }
#cpm-predios .cierre-imagen {
  display:none;width:100%;aspect-ratio:21/9;background-size:cover;background-position:center;
  margin-bottom:clamp(44px,5.5vw,72px);
}
#cpm-predios .cierre.con-foto .cierre-imagen { display:block }
#cpm-predios .cierre-cabeza { padding-top:clamp(56px,7vw,92px) }
#cpm-predios .cierre.con-foto .cierre-cabeza { padding-top:0 }
#cpm-predios .cierre h2 { margin:0 auto 20px;max-width:26ch;text-wrap:balance }
#cpm-predios .cierre .entradilla { margin:0 auto;text-align:center }
#cpm-predios .cierre .acciones { justify-content:center }
#cpm-predios .cierre .rotulo { justify-content:center }

/* ---------- Documentos del bloque institucional ----------
   Cuando hay fotografia de documento se muestran; si no, se queda el sello
   giratorio. Mismo criterio que las escenas: ningun hueco vacio. */
#cpm-predios .documentos { display:none;grid-template-columns:1fr 1fr;gap:clamp(12px,1.6vw,18px) }
#cpm-predios .institucional.con-docs .documentos { display:grid }
#cpm-predios .institucional.con-docs .sello-caja { display:none }
#cpm-predios .doc-figura { margin:0 }
#cpm-predios .doc-lamina {
  aspect-ratio:3/4;background-size:cover;background-position:top center;
  border:1px solid rgba(255,255,255,.34);background-color:#fff;
  box-shadow:0 20px 46px rgba(0,0,0,.34);
  opacity:0;transform:translateY(16px);
  transition:opacity .7s cubic-bezier(.22,.61,.36,1),transform .7s cubic-bezier(.22,.61,.36,1);
}
#cpm-predios .institucional.is-visible .doc-lamina { opacity:1;transform:none }
#cpm-predios .institucional.is-visible .doc-figura:nth-child(2) .doc-lamina { transition-delay:.12s }
#cpm-predios .doc-figura figcaption {
  margin-top:10px;font-size:11px;letter-spacing:.12em;text-transform:uppercase;
  color:#9fb0dd;line-height:1.5;
}
#cpm-predios .cierre-legal {
  margin-top:clamp(34px,4vw,48px);padding-top:24px;border-top:1px solid var(--linea);
  font-size:13px;color:var(--suave);max-width:78ch;margin-left:auto;margin-right:auto;
}

/* ---------- Entradas al hacer scroll ----------
   Solo se ocultan si el JS arranco (.js-on). Si el motor falla, todo el
   contenido queda visible: nunca una pagina en blanco. */
#cpm-predios.js-on .rv { opacity:0;transform:translateY(20px) }
#cpm-predios.js-on .rv.is-visible {
  opacity:1;transform:none;
  transition:opacity .68s cubic-bezier(.22,.61,.36,1),transform .68s cubic-bezier(.22,.61,.36,1);
}
#cpm-predios.js-on .rv.d1.is-visible { transition-delay:.08s }
#cpm-predios.js-on .rv.d2.is-visible { transition-delay:.16s }
#cpm-predios.js-on .rv.d3.is-visible { transition-delay:.24s }
/* Paralaje suave de las fotos: el guion escribe --par (en px). */
#cpm-predios .escena-foto,#cpm-predios .escena-foto-2 { will-change:transform }
#cpm-predios .escena.con-foto.is-visible .escena-foto {
  transform:scale(1.04) translate3d(0,var(--par,0px),0);
}

@media (prefers-reduced-motion:reduce) {
  #cpm-predios.js-on .rv { opacity:1;transform:none;transition:none }
  #cpm-predios .btn,#cpm-predios .caso,#cpm-predios .ficha { transition:none }
  #cpm-predios .institucional.is-visible .sello-anillo { animation:none }
  #cpm-predios .escena.is-visible .trazo,#cpm-predios .escena.is-visible .aparece,
  #cpm-predios .escena.is-visible .sello,#cpm-predios .escena.is-visible .escena-marco,
  #cpm-predios .servicio.is-visible .icono path { animation:none }
  #cpm-predios .trazo { stroke-dashoffset:0 }
  #cpm-predios .aparece,#cpm-predios .sello { opacity:1 }
  #cpm-predios .escena-marco { clip-path:none }
  #cpm-predios .escena.con-foto.is-visible .escena-foto { transform:none }
}

/* ---------- Adaptable ---------- */
@media (max-width:1080px) {
  #cpm-predios .servicios { grid-template-columns:repeat(2,1fr) }
  #cpm-predios .institucional { grid-template-columns:1fr;gap:30px }
  #cpm-predios .sello-caja { max-width:260px;margin:0 auto }
}
@media (max-width:900px) {
  #cpm-predios .portada-rejilla { grid-template-columns:1fr;gap:30px }
  #cpm-predios .portada .escena { aspect-ratio:16/11;min-height:0 }
  #cpm-predios .casos-menores,#cpm-predios .alcance { grid-template-columns:1fr }
  #cpm-predios .caso,#cpm-predios .caso--espejo { grid-template-columns:1fr }
  /* Vertical tambien en movil: las fotos de los casos son verticales y en
     un hueco apaisado se recortaban por la mitad. */
  #cpm-predios .caso .escena,#cpm-predios .caso--espejo .escena {
    order:0;aspect-ratio:4/5;min-height:0;height:auto;
    border-right:0;border-left:0;border-bottom:1px solid var(--linea);
  }
  #cpm-predios .documentos { grid-template-columns:1fr 1fr }
  #cpm-predios .alcance .escena { aspect-ratio:16/10;min-height:0 }
  #cpm-predios .institucional-lista { grid-template-columns:1fr;gap:18px }
  #cpm-predios .portada-datos { gap:20px 30px }
  #cpm-predios .dato { min-width:44% }
}
@media (max-width:560px) {
  #cpm-predios { font-size:15.5px }
  #cpm-predios .servicios { grid-template-columns:1fr }
  #cpm-predios .acciones .btn { width:100%;justify-content:center }
  #cpm-predios .pasos { padding-left:34px }
  #cpm-predios .via { left:9px }
  #cpm-predios .paso { grid-template-columns:1fr;gap:8px }
  #cpm-predios .paso-num { font-size:20px }
  #cpm-predios .paso-num::before { left:-33px;top:.28em }
  #cpm-predios .caso .escena { aspect-ratio:16/9 }
  #cpm-predios .caso-menor { grid-template-columns:1fr;gap:14px }
  #cpm-predios .portada-datos { flex-direction:column;gap:16px }
  #cpm-predios .dato { min-width:0 }
}
</style>
